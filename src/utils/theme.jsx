import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#9C27B0",
    },
    secondary: {
      main: "#fff",
    },
    error: {
      main: "#FF4949",
    },
  },
});

export default theme;
